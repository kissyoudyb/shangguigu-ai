flows:
  # 测试用功能
  switch_user_id:
    name: 切换账号
    description: 切换账号
    persisted_slots:
      - user_id
    steps:
      - collect: user_id
        description: 用户id
#        ask_before_filling: true  # 保证每次都触发utter